{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/paystack/(.*)", "dest": "/api/paystack" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}